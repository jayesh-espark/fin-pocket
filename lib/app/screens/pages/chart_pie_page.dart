import 'package:flutter/material.dart';

class ChartPieScreen extends StatelessWidget {
  const ChartPieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Chart Pie")));
  }
}
